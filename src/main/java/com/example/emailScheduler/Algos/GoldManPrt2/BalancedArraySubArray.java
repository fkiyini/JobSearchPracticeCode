package com.example.emailScheduler.Algos.GoldManPrt2;

public class BalancedArraySubArray {
    public static boolean aBalancedArraySubArray(int [] arr){
       int sum = 0;
        for(int i:arr)
            sum+=i;
        if(sum%2 != 0 ) return false;
        else {
//            for
        }
        return false;
    }
}
